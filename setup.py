from setuptools import setup

setup(
   name='laplaceae',
   version='0.1',
   description='Laplace auto encoder',
   author='foo',
   author_email='foomail@foo.com',
   packages=['src'],  #same as name
)